# Netlify-Konfiguration – Build & SPA-Routing
[build]
  command = "npm run build"
  publish = "dist"

# Alle Pfade auf index.html umleiten (React-Router übernimmt das Routing)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
